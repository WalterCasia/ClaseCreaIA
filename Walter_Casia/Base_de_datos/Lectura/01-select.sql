-- select

-- todos los datos de la tabal users
SELECT * FROM user;

-- solo nombre de los usurarios
SELECT name FROM user;

-- solo el nombre y el email de los usuarios
SELECT name, email FROM users;
